# 🧠 1.1.2 Memória sob pressão
**Categoria:** Infraestrutura  
**CI afetado:** Servidor SQL / Instância SQL  
**Escopo:** Engine / Sistema Operacional  
**Severidade sugerida:** P1 / P2  

---

## 🎯 Objetivo

Identificar pressão de memória no SQL Server e no Sistema Operacional, analisando grants, consumo interno e disponibilidade geral.

---

## 🔎 Diagnóstico Inicial (Quick Triage)

Ferramenta opcional:

```sql
EXEC sp_whoisactive;

SELECT * 
FROM sys.dm_exec_query_memory_grants;

SELECT * 
FROM sys.dm_os_process_memory;
```
<br><br>

🧠 **Análise por DMV**
- **Grants de Memória Ativos**
```sql
SELECT *
FROM sys.dm_exec_query_memory_grants;
```
Objetivo:

- Identificar consultas aguardando memória
- Detectar RESOURCE_SEMAPHORE

<br><br>

- **Consumo Interno de Memória (Clerks)**
```sql
SELECT *
FROM sys.dm_os_memory_clerks
ORDER BY pages_kb DESC;
```
Objetivo:

-Identificar componentes internos com maior consumo

<br><br>

- **Memória do Processo SQL Server**
```sql
SELECT *
FROM sys.dm_os_process_memory;
```
Objetivo:

- Validar pressão interna da instância

<br><br>

- **Memória Disponível no Sistema Operacional**
```sql
SELECT *
FROM sys.dm_os_sys_memory;
```
Objetivo:

- Avaliar memória livre no SO
- Identificar pressão externa ao SQL Server

---

🖥️ **PerfMon (Sistema Operacional)**

Principais contadores:

- SQLServer:Memory Manager\Memory Grants Pending
- SQLServer:Buffer Manager\Page Life Expectancy
- Memory\Available MBytes

Objetivo:

- Confirmar pressão real de memória
- Diferenciar gargalo SQL vs Sistema Operacional

---

🧰 **ScriptDBA / Ferramentas Opcionais**

Ferramenta não nativa:

- sp_whoisactive

Uso recomendado para análise rápida de grants e sessões.

---

🧩 **Possíveis Causas**

- Consultas com alto consumo de memória
- Falta de índices adequados
- Paralelismo excessivo
- Configuração inadequada de Max Server Memory
- Pressão externa no servidor

---

🛠️ **Ações de Mitigação**

- Identificar sessões com grants elevados
- Avaliar planos de execução
- Ajustar Max Server Memory (quando necessário)
- Revisar índices e estatísticas
- Reduzir paralelismo em consultas críticas

---

🔁 **Monitoramento Pós-Mitigation**
```sql
SELECT *
FROM sys.dm_exec_query_memory_grants;

SELECT *
FROM sys.dm_os_process_memory;
```
